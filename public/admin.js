const fmt=n=>new Intl.NumberFormat("en-NG",{style:"currency",currency:"NGN",maximumFractionDigits:0}).format(n);
let editing=null;
async function load(){const s=await fetch("/api/stats").then(r=>r.json());document.querySelector("#productsN").textContent=s.products;document.querySelector("#ordersN").textContent=s.orders;document.querySelector("#revenue").textContent=fmt(s.revenue);document.querySelector("#customers").textContent=s.customers;document.querySelector("#low").textContent=s.lowStock;const ps=await fetch("/api/products").then(r=>r.json());document.querySelector("#productTable").innerHTML=`<table class="table"><tr><th>Image</th><th>Product</th><th>Category</th><th>Price</th><th>Stock</th><th>Action</th></tr>${ps.map(p=>`<tr><td><img class="thumb" src="${p.image}"></td><td>${p.name}</td><td>${p.category}</td><td>${fmt(p.price)}</td><td>${p.stock}</td><td><button class="action" onclick='edit(${JSON.stringify(p)})'>Edit</button> <button class="action" onclick="del(${p.id})">Delete</button></td></tr>`).join("")}</table>`;document.querySelector("#bars").innerHTML=s.byCategory.map(x=>`<div class="barwrap"><div class="bar" style="height:${Math.max(8,x.count*35)}px"></div><small>${x.category}</small><b>${x.count}</b></div>`).join("");document.querySelector("#orders").innerHTML=s.recent.length?s.recent.map(o=>`<p><b>#${o.id}</b> ${o.customer} — ${fmt(o.total)} — <span>${o.status}</span></p>`).join(""):"<p>No orders yet.</p>"}
function showForm(){editing=null;document.querySelector("#formTitle").textContent="Add product";document.querySelectorAll("form input,form textarea").forEach(x=>x.value="");document.querySelector("#modal").classList.add("show")}
function hideForm(){document.querySelector("#modal").classList.remove("show")}
function edit(p){editing=p;showForm();document.querySelector("#formTitle").textContent="Edit product";for(const k of ["name","category","price","stock","image","description"])document.querySelector("#"+k).value=p[k]||""}
async function saveProduct(e){e.preventDefault();const body={name:name.value,category:category.value,price:price.value,stock:stock.value,image:image.value,description:description.value};await fetch(editing?"/api/products/"+editing.id:"/api/products",{method:editing?"PUT":"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});hideForm();load()}
async function del(id){if(confirm("Delete this product?")){await fetch("/api/products/"+id,{method:"DELETE"});load()}}
load();